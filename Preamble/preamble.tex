%% Encodings packages
\usepackage[utf8]{inputenc} % Specify an input encoding UTF8
\usepackage[T1]{fontenc}    % Font encodings
%% Math packages
\usepackage{amsmath,amssymb,amsthm}
\usepackage{makeidx}
%% Figures
\usepackage{graphicx}		% Including Graphics Files, delete the demo option if you want to include your owen Figs
\usepackage{epstopdf}	    % adds support of handling eps images
\usepackage{float} 			% Force the placement of Figs 
% Improve float placement behavior so figures don't all float to the end
% `placeins` on this TeXLive doesn't accept the `chapter` option, so load it
% normally and insert FloatBarriers at chapter boundaries using `etoolbox`.
\usepackage{placeins} % prevent floats from moving past explicit barriers
\usepackage{etoolbox}
\preto\chapter{\FloatBarrier}% force floats to be processed before each chapter
\usepackage{flafter} % ensure floats appear after their definitions
% Allow more floats on a page and be more permissive about float pages
\renewcommand{\topfraction}{.85}
\renewcommand{\textfraction}{.1}
\renewcommand{\floatpagefraction}{.75}
\setcounter{topnumber}{5}
\setcounter{bottomnumber}{5}
\setcounter{totalnumber}{10}
\setcounter{dbltopnumber}{5}
%% Language
\usepackage[english,main=french]{babel} % Set the language
%\addto\captionsfrench{\def\tablename{Tableau}}
%% Page layout
\usepackage[left=25mm,right=25mm,top=35mm,bottom=30mm]{geometry} % Margines
\usepackage{afterpage}
%% Set the Footer and the Header
\usepackage{fancyhdr}
\fancypagestyle{plain}{%
\fancyhf{} % clear all header and footer fields
\cfoot{\thepage} % the page number in the foot center
\renewcommand{\headrule}{\hrule height 0pt \vspace{0mm}\hrule height 0pt} % remove plain header rule
}  % reset the page style bcz the arabicore.sty was loaded in the babel

\newcommand{\changefont}{%
	\fontsize{9}{11}\selectfont
}
\fancyhf{}
\pagestyle{fancy}
%\fancyhead[LE,RO]{\changefont \slshape \rightmark} %section
\fancyhead[RE,LO]{\changefont \slshape \leftmark} %chapter
\fancyfoot[C]{\changefont \thepage} %footer
% Table
\usepackage{array}	    % Fixed length either for each column or for the entire table
\usepackage{tabularx}	% Control the width the entire table
\usepackage{multirow}	% Merged rows and columns
\usepackage{longtable}	% Insert a very long table,
\usepackage{booktabs}   % To thicken table lines
\newcommand{\tabmultirow}[2]{\multirow{#1}{=}{#2}}
%% Space line
\usepackage{setspace}
\onehalfspacing
%% Customization of captions
\usepackage[font=small,labelfont=bf]{caption} 
\captionsetup[table]{name=Tableau}			  
\captionsetup[figure]{name=Figure}			
% Lists with options (e.g., [nosep,left=0pt])
\usepackage{enumitem}
\usepackage[nottoc]{tocbibind} %list of figures and list of tables are added in the toc
\usepackage{subcaption}
%\usepackage{subfig}
\usepackage[lined,ruled,vlined,french,algochapter,dotocloa]{algorithm2e} % For writing algorithms
%%
\usepackage{appendix}

%% Generate Random Text
\usepackage{blindtext}
\usepackage{lipsum}
%%
\usepackage{siunitx}[=v2] 
\usepackage{ifthen}
\usepackage{longtable}
%% List of Symbols
\usepackage[intoc]{nomencl}
\makenomenclature
\renewcommand{\nomgroup}[1]{%
	\ifthenelse{\equal{#1}{R}}{\item[\textbf{Symboles Romains}]}{%
		\ifthenelse{\equal{#1}{G}}{\item[\textbf{Symboles Grecs}]}{%
			\ifthenelse{\equal{#1}{A}}{\item[\textbf{Acronymes / Abréviations}]}{%
				\ifthenelse{\equal{#1}{S}}{\item[\textbf{Autres Symboles}]}{% 
				}% Mathematical symbols > S
			}% Abbreviations        > A
		}% Greek Symbols        > G
	}% Roman Symbols        > R
}

\setlength{\nomlabelwidth}{3cm}
%%
\usepackage{pdfpages} % insertion of external multi-page PDF or PS documents
\usepackage{cite}
%% New Environment
\theoremstyle{plain}
\newtheorem{theorem}{Théorème}[chapter]

\theoremstyle{definition}
\newtheorem{definition}{Définition}[chapter]

\theoremstyle{remark}
\newtheorem*{remark}{Remarque}

\newenvironment{dedication}
{
	\cleardoublepage
	\thispagestyle{empty}
	\vspace*{\stretch{1}}
	\hfill
	\begin{minipage}[t]{0.66\textwidth}
		\raggedright
	}{\end{minipage}\vspace*{\stretch{3}}\clearpage}

\setcounter{secnumdepth}{3}
\usepackage{tocvsec2} % Used to delete the sections of the appendix from the ToC
\usepackage{pifont}  % More styles for bullets
\usepackage{bm}		 % Access bold math symbols
%% Referring to Figure and Table in the text (you can use the classical way)
\newcommand{\fig}[1]{Figure.~\ref{#1}}
\newcommand{\tab}[1]{Tableau.~\ref{#1}}
%%
\usepackage{hyperref}
\usepackage{xurl}
\urlstyle{tt}
\DeclareRobustCommand{\texttt}[1]{\nolinkurl{#1}}
\hypersetup{
	final=true,
	plainpages=false,
	pdfstartview=FitV,
	pdftoolbar=true,
	pdfmenubar=true,
	bookmarksopen=true,
	bookmarksnumbered=true,
	breaklinks=true,
	linktocpage,
	colorlinks=true,
	linkcolor=blue,
	urlcolor=blue,
	citecolor=blue,
	anchorcolor=black
}

\usepackage{xcolor}


% ── Préambule — à ajouter une seule fois ────────────────────
\usepackage{listings}
\usepackage{xcolor}

% \lstdefinestyle{json}{
%   basicstyle=\ttfamily\footnotesize,
%   breaklines=true,
%   breakatwhitespace=false,
%   columns=fullflexible,
%   keepspaces=true,
%   frame=single,
%   rulecolor=\color{gray!40},
%   backgroundcolor=\color{gray!5},
%   numbers=left,
%   numberstyle=\tiny\color{gray},
%   numbersep=8pt,
%   showstringspaces=false,
%   stringstyle=\color{orange!80!black},
%   keywordstyle=\color{blue!70!black},
%   commentstyle=\color{green!50!black},
%   captionpos=b,
%   aboveskip=12pt,
%   belowskip=6pt,
%   literate=
%     {0}{{{\color{purple!80!black}0}}}{1}
%     {1}{{{\color{purple!80!black}1}}}{1}
%     {2}{{{\color{purple!80!black}2}}}{1}
%     {3}{{{\color{purple!80!black}3}}}{1}
%     {4}{{{\color{purple!80!black}4}}}{1}
%     {5}{{{\color{purple!80!black}5}}}{1}
%     {6}{{{\color{purple!80!black}6}}}{1}
%     {7}{{{\color{purple!80!black}7}}}{1}
%     {8}{{{\color{purple!80!black}8}}}{1}
%     {9}{{{\color{purple!80!black}9}}}{1}
% }

\usepackage[utf8]{inputenc}
\lstset{inputencoding=utf8, extendedchars=true}

\usepackage{minted}
\usepackage{caption}
\usepackage{mdframed}

% Custom float for long code listings that break across pages
\newenvironment{longlisting}
  {\captionsetup{type=listing}}
  {}
\usepackage[most, minted]{tcolorbox}

\usepackage{pgfplots}
\pgfplotsset{compat=1.18}